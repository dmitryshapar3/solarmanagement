services:
  deye-solar:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal,1433;Database=DeyeSolar;User Id=sa;Password=Dev@Password123!;TrustServerCertificate=True;MultipleActiveResultSets=true
    restart: unless-stopped
